APP_CONFIG = {
    "project": "OpenClaw Debug Gate",
    "mode": "training",
    "allow_debug_panel": True,
    "command_prefix": "/"
}

DEBUG_INFO = {
    "gateway": "training-gateway-01",
    "build": "2026.3.11-training",
    "notes": "Owner-only debug surface. Operator must not read this."
}